export const loadingActionHandler = <T>(
  state: T,
  payload: boolean,
  prefix: string,
  data: null = null,
  error: null = null,
) => {
  // state[`${prefix}Error`] = error;
  // state[`${prefix}Data`] = data;
  // state[`${prefix}Loading`] = payload;
};
