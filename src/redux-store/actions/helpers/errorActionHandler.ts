export const errorActionHandler = <T>(
  state: T,
  error: object | null,
  prefix: string,
  data: null = null,
) => {
  // state[`${prefix}Data`] = data;
  // state[`${prefix}Error`] = error;
  // state[`${prefix}Loading`] = false;
};
