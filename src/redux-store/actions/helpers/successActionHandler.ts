export const successActionHandler = <T>(
  state: T,
  payload: object[] | null,
  prefix: string,
  error: null = null,
) => {
  // state[`${prefix}Error`] = error;
  // state[`${prefix}Data`] = payload;
  // state[`${prefix}Loading`] = false;
};
